<UserControl x:Class="PdfDisplay.FirstStartView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:telerik="http://schemas.telerik.com/2008/xaml/presentation"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="0.5*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Vertical" HorizontalAlignment="Center" Grid.Row="1">
            <TextBlock FontSize="20" Foreground="#FF7B7B7B" Text="Browse for a PDF file that you want to see."/>
            <Rectangle Height="10"/>
            <telerik:RadButton Click="RadButton_Click" Content="Browse ..." Width="150" Height="32"
                               HorizontalAlignment="Left" Background="#FF80397B"  Foreground="#FFF7F7F7"/>
        </StackPanel>    
    </Grid>
</UserControl>
